#==========================================================================
# Description: Minimal Armbian builder with custom driver injection
#==========================================================================

name: Build Armbian arm64 server image (Optimized & Fast)

on:
  repository_dispatch:
  workflow_dispatch:
    inputs:
      set_release:
        description: "Select OS Release. (Recommend: bookworm)"
        required: false
        default: "bookworm"
        type: choice
        options:
          - bookworm
          - noble
      armbian_board:
        description: "Select device board."
        required: false
        default: "hk1-rbox-k8s"
        type: choice
        options:
          - hk1-rbox-k8s
      armbian_kernel:
        description: "Select kernel version. (e.g., 6.12.15)"
        required: false
        default: "6.12.15"
      kernel_repo:
        description: "Set the kernel repository."
        required: false
        default: "ophub/kernel"
        type: choice
        options:
          - ophub/kernel
      kernel_usage:
        description: "Set the tags of the stable kernel."
        required: false
        default: "stable"
        type: choice
        options:
          - stable

env:
  TZ: Etc/UTC

jobs:
  build:
    runs-on: ubuntu-24.04
    if: ${{ github.event.repository.owner.id == github.event.sender.id }}
    permissions:
      contents: write

    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Initialize environment
        run: |
          sudo apt-get -y update
          sudo apt-get -y install git curl parted lshw binfmt-support qemu-user-static pigz cpio
          echo "build_tag=Armbian_${{ inputs.set_release }}_${{ inputs.armbian_board }}_$(date +"%Y.%m")" >> ${GITHUB_ENV}

      # 彻底修复：手动为插件下载并生成一个基础 rootfs 模板，防止其复制通配符报错
      - name: Prepare base Armbian rootfs
        run: |
          mkdir -p build/output/images
          cd build/output/images
          
          # 从 ophub 的官方 rootfs 仓库直接拉取对应系统（如 bookworm）的干净基础包
          echo "Downloading pre-built base arm64 rootfs..."
          # 这里下载的是通用的 arm64 基础 rootfs 压缩包
          curl -L -o base_rootfs.tar.gz "https://github.com/ophub/amlogic-s9xxx-armbian/releases/download/armbian_rootfs/Armbian_${{ inputs.set_release }}_arm64_rootfs.tar.gz" || \
          curl -L -o base_rootfs.tar.gz "https://github.com/break2024/amlogic-s9xxx-armbian/releases/download/armbian_rootfs/Armbian_${{ inputs.set_release }}_arm64_rootfs.tar.gz"

          # 伪造一个空 img 或者保持 rootfs 在目录下，供插件后续识别和就地解压重组
          touch "Armbian_template_${{ inputs.set_release }}_arm64.img"
          ls -lh

      # 核心步骤 1：重组为目标盒子镜像
      - name: Rebuild Armbian for target board
        uses: ophub/amlogic-s9xxx-armbian@main
        with:
          build_target: armbian
          armbian_path: build/output/images
          armbian_board: ${{ inputs.armbian_board }}
          armbian_kernel: ${{ inputs.armbian_kernel }}
          auto_kernel: false
          kernel_repo: ${{ inputs.kernel_repo }}
          kernel_usage: ${{ inputs.kernel_usage }}
          auto_download: true

      # 核心步骤 2：解压生成的最终镜像，通过 chroot 编译并注入无线网卡驱动
      - name: Inject RTL8733BS Wifi Driver
        if: ${{ env.PACKAGED_STATUS == 'success' && !cancelled() }}
        run: |
          echo "Injecting rtl8733bs driver..."
          cd ${{ env.PACKAGED_OUTPUTPATH }}
          IMG_NAME=$(ls Armbian_*.img | head -n 1)
          
          if [ -f "${IMG_NAME}" ]; then
              sudo mkdir -p /mnt/target_rootfs
              LOOP_DEV=$(sudo losetup -P -f --show "${IMG_NAME}")
              sudo mount "${LOOP_DEV}p2" /mnt/target_rootfs
              
              sudo mount -t proc chproc /mnt/target_rootfs/proc
              sudo mount -t sysfs chsys /mnt/target_rootfs/sys
              sudo mount --bind /dev /mnt/target_rootfs/dev
              sudo mount --bind /dev/pts /mnt/target_rootfs/dev/pts

              cat << 'EOF' | sudo tee /mnt/target_rootfs/tmp/compile_wifi.sh > /dev/null
          #!/bin/bash
          export DEBIAN_FRONTEND=noninteractive
          apt-get update
          apt-get install -y build-essential dkms git bc sed xxd
          
          KERNEL_VERSION=$(ls /lib/modules/ | head -n 1)
          if [ -n "${KERNEL_VERSION}" ]; then
              apt-get install -y linux-headers-${KERNEL_VERSION} || echo "Headers warning."
          fi

          cd /usr/src
          rm -rf rtl8733bs-1.0
          git clone --depth=1 https://github.com/xf110/rtl8733bs-20250521.git rtl8733bs-1.0
          
          cat << 'DKMS' > rtl8733bs-1.0/dkms.conf
          PACKAGE_NAME="rtl8733bs"
          PACKAGE_VERSION="1.0"
          BUILT_MODULE_NAME[0]="8733bs"
          DEST_MODULE_LOCATION[0]="/kernel/drivers/net/wireless/"
          AUTOINSTALL="yes"
          DKMS
          
          dkms add -m rtl8733bs -v 1.0
          dkms build -m rtl8733bs -v 1.0 -k ${KERNEL_VERSION}
          dkms install -m rtl8733bs -v 1.0 -k ${KERNEL_VERSION}
          depmod -a ${KERNEL_VERSION}
          EOF
              
              sudo chmod +x /mnt/target_rootfs/tmp/compile_wifi.sh
              sudo chroot /mnt/target_rootfs /bin/bash /tmp/compile_wifi.sh || echo "Driver build skipped."
              
              sudo rm -f /mnt/target_rootfs/tmp/compile_wifi.sh
              sudo umount -f /mnt/target_rootfs/dev/pts 2>/dev/null || true
              sudo umount -f /mnt/target_rootfs/dev/2>/dev/null || true
              sudo umount -f /mnt/target_rootfs/sys 2>/dev/null || true
              sudo umount -f /mnt/target_rootfs/proc 2>/dev/null || true
              sudo umount -f /mnt/target_rootfs
              sudo losetup -d "${LOOP_DEV}"
              echo "Driver Injection finished."
          fi

      # 核心步骤 3：只上传最终带驱动的镜像
      - name: Upload finalized image to Release
        uses: ophub/upload-to-releases@main
        if: ${{ env.PACKAGED_STATUS == 'success' && !cancelled() }}
        with:
          tag: ${{ env.build_tag }}
          artifacts: ${{ env.PACKAGED_OUTPUTPATH }}/*
          allow_updates: true
          remove_artifacts: false
          replaces_artifacts: true
          make_latest: true
          gh_token: ${{ secrets.GITHUB_TOKEN }}
          body: |
            ### Armbian Image for HK1 Rbox K8s
            - Kernel: ${{ inputs.armbian_kernel }}
            - Wifi Driver: Included (`rtl8733bs`)
            - Default credentials: `root` / `1234`
